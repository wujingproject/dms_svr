'use strict';

module.exports = (app) => {
    const { STRING, INTEGER, DATE } = app.Sequelize;

    // const Demo1Model = app.model.define('dormitory_info', {
    //     id: { type: INTEGER, primaryKey: true, autoIncrement: true },
    //     name: STRING(30),
    //     age: INTEGER,
    //     created_at: DATE,
    //     updated_at: DATE,
    // });

    // return Demo1Model;
};